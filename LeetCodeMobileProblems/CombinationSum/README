Given a set of candidates and a target number, find all unique combinations in the candidates
where candidate numbers sums to target

candidates can be repeated

    positive numbers
    non-descending order
    no duplicate

e.g. 
    candidates 2, 3, 6, 7
    target 7
solution:
    7
    2, 2, 3
